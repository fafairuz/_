How to install CocoaPods (pod)

Open terminal.
```
sudo gem update --system
sudo gem install cocoapods
```

If you have write permission error (esp in High Sierra), run
```
sudo gem install cocoapods -n/usr/local/bin 
```